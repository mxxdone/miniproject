spring:
  datasource:
    url: jdbc:postgresql://miniproject-db.cv0468m20ioy.ap-northeast-2.rds.amazonaws.com:5432/miniproject
    username: ${DB_USERNAME} # 환경 변수 사용
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update # 운영 환경에서는 create, create-drop 사용 금지
  data:
    redis:
      host: localhost
      port: 6379
app:
  oauth2:
    redirect-uri: https://13.209.122.52/oauth2/redirect
admin:
  password: ${ADMIN_PASSWORD}
springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false