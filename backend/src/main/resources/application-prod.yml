spring:
  datasource:
    url: jdbc:postgresql://miniproject-db.cs3y22eqerq5.us-east-1.rds.amazonaws.com:5432/miniproject
    username: ${DB_USERNAME} # 환경 변수 사용
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update # 운영 환경에서는 create, create-drop 사용 금지